# Python API from `python3 serve.py` (default port 8000). Required for `vercel dev` and
# optional for `npm run dev` (Vite can also proxy /api when this is unset — see vite.config.ts).
VITE_API_BASE=http://127.0.0.1:8000
